[project]
name = "task-manager"
version = "0.1.0"
description = "Task scheduling and execution system with quota control"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.uv]
dev-dependencies = ["pytest>=9.0"]
